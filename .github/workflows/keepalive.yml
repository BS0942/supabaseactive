name: Supabase Keep Alive

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase
        run: |
          curl -H "apikey: ${{ secrets.SUPABASE_KEY }}" \
               "https://oumgkvtkyndazspurdph.supabase.co/rest/v1/"
